with Tropos;

package Carthage.Handles.Units.Configure is

   procedure Configure_Unit
     (Config : Tropos.Configuration);

   procedure Configure_Targets
     (Config : Tropos.Configuration);

end Carthage.Handles.Units.Configure;
