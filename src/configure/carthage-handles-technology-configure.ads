with Tropos;

package Carthage.Handles.Technology.Configure is

   procedure Configure_Technology
     (Config : Tropos.Configuration);

   procedure Configure_Tree
     (Config : Tropos.Configuration);

end Carthage.Handles.Technology.Configure;
