with Tropos;

package Carthage.Handles.Worlds.Configure is

   procedure Configure_World
     (Config : Tropos.Configuration);

   procedure Configure_Movement_Costs
     (Config : Tropos.Configuration);

end Carthage.Handles.Worlds.Configure;
